#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char* argv[]){
   FILE* in;  /* file handle for input */  
   FILE* out; /* file handle for output */
   char* A;  /* This is a pointer pointing at the beginning of the array */

/*   while(fscanf(in, "%c", A) != EOF){
      A = (char*)malloc(256 * sizeof(char));
      if (A == NULL){
         fprintf(stderr, "malloc failed \n");
         exit(EXIT_FAILURE);
      }

       To this point, the program requested some memories

      free(A);
   }   */
}

